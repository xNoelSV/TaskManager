server:
  port: 8082

spring:
  application:
    name: task-service
  datasource:
    url: jdbc:h2:mem:tasksdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    username: sa
    password:
  jpa:
    hibernate.ddl-auto: update